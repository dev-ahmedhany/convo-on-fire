import { makeStyles } from "@material-ui/core";
export const useStyles = makeStyles({
  bottomSender: {
    width: "100%",
    padding: "5px 10px",
  },
  textBox: {
    borderRadius: "30px",
    padding: "0 12px 0 0",
  },
});
